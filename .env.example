# Server port (local and remote)
PORT=8080

# Dart VM service port (used by debugger)
DART_VM_PORT=8181

# Remote deploy — fill in on machines that deploy to a server
REMOTE_HOST=user@example.com
SSH_PORT=22
REMOTE_DIR=~/durak
